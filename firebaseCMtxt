-- firebase deploy --only hosting 
// chạy 
--  npm install -g firebase-tools --force
-> cài đặt module firebase-tools

-- firebase login
-> kêt nối với tài khoản firebase

-- firebase logout
-- firebase use --add
-> chọn và sử dụng project trên firebase 

-- firebase init 
-> khởi tạo firebase . chọn hosting -> chọn project trên firebase -> chọn project đc tạo trên local bằng(ng build)

-- firebase deploy
-> chiển khai dự án trên firebase

-- firebase hosting:channel:deploy preview_name
-> chạy thử